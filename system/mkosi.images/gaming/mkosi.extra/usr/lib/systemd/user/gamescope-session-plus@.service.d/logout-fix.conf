[Service]
 # When Cosmic is run first, it sets the WAYLAND_DISPLAY variable.
 # This is not cleared when logging out of Cosmic, which tells Gamescope
 # to try to run itself nested
ExecStartPre=-/usr/bin/systemctl --user unset-environment WAYLAND_DISPLAY

#!/bin/bash
set -e

CONFEXT="$BUILDROOT/usr/lib/confexts/nvidia"
mkdir -p "$CONFEXT"
cp --archive --no-target-directory --update=none "$BUILDROOT/etc" "$CONFEXT/etc"

# move back the depmod files
cd "$BUILDROOT"/usr/lib/modules/*/
cp -a .depmod/. .
rm -rf .depmod

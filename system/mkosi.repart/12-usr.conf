[Partition]
Type=usr
Label=%M_%A
Format=erofs
CopyFiles=/usr:/
Verity=data
VerityMatchKey=usr

# this makes builds fail if they exceed our size limit for /usr
SizeMaxBytes=5G

Minimize=yes
Compression=zstd
SplitName=%t.%U
